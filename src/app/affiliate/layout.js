export const metadata = {
  title: "Affiliate Portal — Body Pharm Labs",
  robots: { index: false, follow: false },
};

export default function AffiliateLayout({ children }) {
  return <>{children}</>;
}
